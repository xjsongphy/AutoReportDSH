// pkumpl-typst --- A Typst template for 'Modern Physics Laboratory' in PKU
//
// 请阅读 `README.md`. 需要确保 font 选项指定的字体已安装! 参见 README.md.
// 发布到 Typst Universe 后使用包入口:
// #import "@preview/unofficial-pku-mpl:0.1.0": *
// 从本地克隆使用: #import "mplts.typ": *
#import "mplts.typ": *

#show: mplts.with(font: "macos")

// 以下仅为本文件方便额外定义的命令, 写报告时不需要.
#let code(it) = raw(it)
#let file(it) = text(weight: "bold", raw(it))

#let releasedate = link(
  "https://github.com/CastleStar14654/PKUMpLtX/releases/tag/v0.1.0",
  [2025/03/15, v2.1.6],
)

// ========== 标题区 ==========
#frontmatter(
  title: "实验题目", // 切合报告内容, 简短明确, 可以不同于讲义
  author: "姓名",
  email: "email@pku.edu.cn", // emailphone: 邮箱; 如只用邮箱可省略 phone
  phone: "(86)152XXXXXXXX",
  affiliation: [北京大学物理学院 #h(1em) 学号: 20000xxxxx],
  date: [（实验日期: #releasedate）], // 正式报告请替换为具体日期
  abstract: [
    此部分为摘要. 200--300字, 说明用什么方法做了什么事, 由此得到什么结果和结论, 有何意义. 摘要中不用缩略词, 不用第一人称.
    #note[本文档为对 #link("https://github.com/CastleStar14654/PKUMpLtX")[PKUMpLtX] 的 Typst 移植版使用示例, 灰色部分为额外针对模板使用的说明或是一些能提升输出效果的琐碎细节. 也请注意查看源文档 #file("demo.typ") 中的注释.]
  ],
  keywords: "关键词1, 关键词2, 共2--4个",
)

// ========== 正文 ==========
= 引言

研究论文引言一般包含以下内容: 所研究领域背景和现状, 有待研究的问题, 本研究的物理目的和主要方法. 引言一定要切合报告正文, 不能漫无目的地介绍背景, 要快速地将读者引导到报告主题上. 引言篇幅可以在较大范围内变化, 但最长不应超过报告文字篇幅的1/3.

引言撰写可以参考实验讲义和资料, 可以概括复述, 但不能抄.
#note[这里是一个对实验教材 #cite(<jindaishiyan>) 的引用示例.]
#note[注意在 `#cite(...)` 前后适当添加空格. 可以连续引用. 引用标记要出现在标点之前 #cite(<GBT7714>). 引用处理使用 Typst 内置的 Hayagriva + CSL. 对于外文期刊, `*.bib` 格式的引用数据很容易从出版商获得; #link("https://scholar.google.com")[Google 学术] 和 #link("https://xueshu.baidu.com")[百度学术] 等文献搜索引擎也提供以 `*.bib` 格式导出的功能. 这里是对首次探测到引力波的报道的引用 #cite(<PhysRevLett.116.061102>).]

= 理论 <sec:theory>

可选, 概括本实验必须的理论. 也可以适当地给出必要的公式, 公式应编号.
#note[
  自然对数的底、虚数单位以及圆周率国标 #cite(<GBT3102.11>) 和 ISO #cite(<ISO80000-2>) 都推荐为正体 (虽然国际学界并不怎么实行这点), 本模板提供了 `ii`, `ee`, `jj`, `uppi` 作为简写, 如

  $ #ee^(#ii#uppi) + 1 = 0. $ <eq1>

  注意公式应作为句子的一部分在末尾给出必要的标点符号. 上式可以交叉引用为 @eq1.
]

#note[
  希腊字母在数学模式中可以直接按名称输入, 例如 #code("$ mu $") ($mu$)、#code("$ alpha $") ($alpha$) 和 #code("$ pi $") ($pi$); 普通文本中则可以写作 #code("#sym.mu") (#sym.mu). 度符号可以写作 #code("#sym.degree") (#sym.degree), 因此摄氏度和微米可分别写成 #code("#sym.degree#text(\"C\")") (#sym.degree#text("C")) 和 #code("#sym.mu#text(\"m\")") (#sym.mu#text("m")).

  物理量的单位使用正体, 物理量符号使用斜体, 例如 #code("$ 1 \"m/s\" $") ($1 "m/s"$)、#code("$ 9.8 \"m/s\"^2 $") ($9.8 "m/s"^2$) 和 #code("#sym.mu#text(\"m\")") (#sym.mu#text("m")). 最简单的做法是把单位写在数学模式之外; 如果必须在数学公式中输入单位, 可使用数学模式的字符串, 例如 #code("$ 9.8 \"m/s\"^2 $") ($9.8 "m/s"^2$). 普通文本中的上标可以使用 #code("#super[2]") (#super[2]).
]

= 实验装置

在此部分需要成段介绍实验方法和条件 (不是罗列操作步骤), 交待清楚到别人能重复你的实验结果的程度. 此外, 还需表明你已尽了最大努力来提高实验精度和结果的可靠性 (简单的不确定度估计可以在此节给出, 复杂一些的可以放到分析讨论部分).

首先应给出一个实验装置示意图. 例如, 如下同学的示意图 @fig:instruments 非常清晰, 值得借鉴 (各关键部件也可标在图中).

#figure(
  image("fig/instruments.png", width: 85%),
  caption: [这个例子展示了如何插入图片以及加说明文字.
    #note[推荐使用相对大小插入图片, 比如设置为 0.8 倍当前区域文字宽度. 推荐对实验装置图和数据图使用矢量图插入.]],
) <fig:instruments>

#note[这里使用了 Typst 的 `@label` 引用语法来自动给出带类型的交叉引用, 注意在引用前后适当使用空格以给出最好的显示效果. 小节, 表格, 公式等都可如此引用, 如: @sec:theory, @ssec:table, @eq1, @tab:table_eg, @app:exercise. 交叉引用的标签尽量取得有意义.]

实验仪器和方法不是像普物实验报告那样将所有实验器材列出, 而是要用介绍功用的方式成段给出. 实验条件不仅是指直接影响实验结果的实验参量, 而且还包括影响实验质量和可靠性的因素, 如室温、空气湿度、基真空、原材料纯度等.

= 结果及讨论

此部分是实验报告的主体, 应占报告篇幅的一半以上.
#note[依自己意愿, 结果与分析部分可以分多个小节, 甚至可以将实验结果和对结果的分析讨论拆分为两节.]

实验结果应尽量以图表的形式给出. 每一个图表都应该是完整的, 即阅读图表时可以不必依赖正文. 以图表为中心叙述实验结果和讨论.

== 表格 <ssec:table>

=== 表格

表是被一系列横线隔开的有序排列的数据, 报告格式要求最上和最下两条横线为双横线.
#note[此双横线格式可以通过 `ruled-table` 函数实现.]
例表为 @tab:table_eg.

#figure(
  kind: table,
  caption: [表格示例.],
  block(width: 100%)[
    #ruled-table(
      table(
        columns: (1fr, 1fr, 1fr, 1fr),
        align: (center, center, left, right),
        [居中], [数值], [靠左], [靠右],
        table.hline(stroke: 0.4pt),
        [a], [0.12], [a], [a],
        [bbb], [-100], [bbb], [bbb],
        [ccccc], [50.8], [ccccc], [ccccc],
      )
    )
    #v(6pt)
    #block(width: 100%)[
      #align(left)[
        #set par(first-line-indent: 0em, spacing: 0pt)
        #text(size: 11pt)[
        ᵃ 表格标题要简明扼要; 注意使用 `ruled-table` 生成首尾双横线的表格. 表格中的脚注应作为表格的一部分放在表后. 为方便, 提供了 `mc` 作为兼容别名. #link("https://www.tablesgenerator.com")[Tables Generator 网站]可以方便地生成 Typst 表格. \
        ᵇ 数值列应根据数据特点选择合适的对齐方式; 如需按小数点对齐, 可以手工设置各单元格的对齐方式. 若需更复杂的小数点对齐, 请参考相关工具文档.
        ]
      ]
    ]
  ],
) <tab:table_eg>

从 @tab:table_eg 可以看出, 对表中各项的注释应作为表的一部分放在表后, 而不是页脚或文尾.
#note[在表格中使用脚注时 Typst 会将注释放在页脚 (与 revtex 把表注放在表尾略有不同), 故本模板推荐把表注作为表格下方的小字直接写出. 正文中 Typst 使用普通页脚脚注 (revtex 会将正文脚注并入参考文献, 本模板略有不同).]

当原始数据和处理过的数据常需要出现在同一表中时, 用软件来处理会非常方便. 但出现在实验报告中的表应具有上面给出的例子的格式.

== 数据图

*每个图应尽量让读者不看正文就能基本理解图的含意.* 应包含: 图名、轴名、轴、刻度、标尺、数据点、曲线、图例、标注和图注等部分. 最常用的作图软件有 Origin 或 MATLAB. 学习使用基本的数据处理和作图工具软件也是课程的基本内容. 课程也鼓励大家使用 Python 语言编程作图. 逐点测量得到的函数关系要同时用表格和图给出.
#note[数据点过多可以将数据放到附录. 这里 "逐点测量得到的函数关系" 还是要自己把握一下, 像那种几千个数据点的能谱当然就没必要放原始数据表了.]
*需要作比较的多条曲线要画在同一图上.* 为避免读者在图表和正文间反复跳跃阅读, 正文叙述应紧邻图表, 正文中也要对图表作必要的说明.

#figure(
  image("fig/figsample.pdf", width: 80%),
  caption: [这是数据图的例子.
    #note[在图的标题中应简要说明图中表达的内容, 并对各种符号、线型、颜色的意义做出说明. 如果有多格数据图, 应清晰地分别做出解释说明. 图中的关键性文字 (比如轴名和图例) 的大小最好能和说明文字中的文字大小相当. 关于坐标轴名和单位的标法, 可以参看 #link("https://journals.aps.org/authors/axis-labels-and-scales-on-graphs-h18")[美国物理学会的说明]. 本图是用脚本 #file("figgen.py") 生成的.]],
) <fig:data>

@fig:data 是一张例图.

== 对分析的要求

对于预料之外的实验结果, 必须首先小心证明其可靠性. 读者只有在相信你的实验结果时才愿意花时间看你的分析.

*必须用文字归纳整理出正式的实验结果或结论.* 可信的实验结果是课程报告最重要的内容. 作为一个实验物理工作者, 分析解释出错并不丢脸, 实验结果不被采信则是致命的. 教学实验的结论往往是预先知道的. 所以, 教师更关心的是你的说理过程. 一般说来, 单由课内实验的结果不足以能得到明确的结论. 此时, 你可以引用他人的研究结果来帮助帮助自己的论证, 但必须注明出处. 确实不能得到明确结论时, 可以给出几种可能结论并指出可以再做哪些实验来帮助作进一步的判断. 总之, 分析讨论部分要做到: 论据要valid, 论证要reasonable, 结论要convincing.

= 结论

*文字写一段.* 首先要给出实验结果, 然后再给出由实验结果分析得到的结果和结论. 此部分给出的内容要比摘要中的全面, 用词要更准确.

#pagebreak()

#acknowledgments[
  感谢对实验和报告有具体重要帮助的, 又没被列为作者的人.
  #note[撰写致谢时请使用 #text(style: "italic")[acknowledgments] 函数.]
]

#bibliography-title[参考文献]
#bibliography("bibli.bib", title: none, style: "american-physics-society.csl")

#pagebreak()

// ========== 附录 ==========
#start-appendix()

= 思考题 <app:exercise>
== 可以把每道思考题的题目分别作为小标题
然后书写解答.

= 近代物理实验报告写作要求

物理实验的结果最终需要以论文或报告的形式向同行或公众报道. 外界也主要基于这些论文或报告来评价一个实验物理工作者. 所以, 课程实验报告的撰写是北大 "近代物理实验" 课的一个重要内容和主要的评分依据.

北大近代物理实验课要求按研究论文的形式来撰写课程实验报告. 和任何期刊一样, 近代物理实验课对学生提交的课程实验报告也有内容和格式上的要求. 学生应依照课程提供的课程实验报告模板来撰写自己的实验报告.

课程提供的课程实验报告模板综合了 American Institute of Physics (AIP) 期刊和中国《物理学报》对稿件格式的要求.

本实验报告模板各部分的文字给出了相应节写作时应注意的问题.

#heading(level: 2, numbering: none)[报告写作的一般事项]

+ 课程实验报告应假定读者既不是已知全部实验细节的指导教师, 也不是缺少专业知识的公众, 而是同领域的实验研究者, 或审稿人. 不能要求读者要在读过课程讲义后才能读懂课程实验报告.
+ 文本和物理量单位用正体, 物理变量符号用斜体, 矢量矩阵符号用黑斜体.
  #note[(Typst 中可用 `upright(..)` / `rm(..)` 等给出正体物理量符号.)]
+ 使用国际标准的缩略词, 符号和法定计量单位时应全文一致, 正文中的缩略词在*首次出现时写出全称*, 后附缩略词, 并用括号括起; 之后直接用缩略词, 不再写全称, 如 American Institute of Physics (AIP).
+ 全文标点符号除 "顿号" 外, 其他用英文半角标点符号.
  #note[(推荐的格式中, 英文单词和数字应与汉字之间插入一个空格; 半角标点符号应如英文一般, 在逗号, 句号, 分号等后方插入一个空格; 在引号, 括号外侧各插入一个空格; 连续出现的标点符号之间的多余空格则应删除. Typst 基本上自动为你在最终的 PDF 文稿中完成这些事情. 此外, 如果你觉得半角符号配中文太丑, 请参见 `README.md` 中介绍的标点选项 `quanjiao`)]
+ 公式、图和表要分别用阿拉伯数字编列序号.
  #note[(这点 Typst 可以为你代劳)]
  公式和图表要达到可发表的质量.
+ 凡不是自己独立思考得到的内容都应该引参考文献. 不能大段引用同一参考文献. 对复杂问题, 应该优先考虑引用参考文献得到结果. 对简单一些的问题才鼓励独立思考. 只能引用正式出版物, 不能引用他人实验报告.
+ 模板中的未尽事项可以参考 AIP Style Manual 4th-edition (可从课程网站下载).
  #note[(也可参考 revtex4-2 的文档)]
+ 较长的推导和说明可以作为附件提交, 不占用报告篇幅.
+ 思考题不是报告的组成部分. 应另起一页附在报告的最后.
  #note[(比如作为附录)]

= 对 Typst 中标点输入的额外说明

Typst 中对 dash 有所区分, 连字符用单个 `-` (如 `co-operate` → co-operate), 数字范围用 `--` (如 `14--19` → 14--19), 破折号用 `---` (如 `Yes---or no?` → Yes---or no?), 减号在数学模式中 (如 `$-1$` → $-1$). 中文破折号 (——) 就按一般习惯的用输入法输入即可.

#figure(
  kind: table,
  caption: none,
  ruled-table(
    table(
      columns: (1fr, 1fr, 1fr),
      align: center,
      [*类型*], [*输入*], [*输出*],
      table.hline(stroke: 0.4pt),
      [连字符 hyphen], [`co-operate`], [co-operate],
      [连接号 en-dash], [`14--19`], [14--19],
      [英文破折号 em-dash], [`Yes---or no?`], [Yes---or no?],
      [减号], [`$-1$`], [$-1$],
    )
  )
)

Typst 中半角引号直接输入即可, 如 `'...'` (单引号) 与 `"..."` (双引号). 美式连续嵌套引号等可借助 Unicode 字符直接给出. 此外, 如果想使用全角符号同时想使用实心点格式的句号, 可以使用 `quanjiao` 选项. 源文档中的 "。" 会被自动替换成 "．". 这时, 也就可以风格统一地直接使用中文输入法给出的引号 ""'' 而不用管前面提到的映射了. 具体请参见 `README.md` 中介绍的标点选项.

#note[其实 “ 和 ‘ 以及 ” 和 ’ 分别是同一个 Unicode 字符, 只是被分配了不同的字体.]

= DIY 字体效果测试

下面分别展示衬线, 无衬线和等宽中文字体效果, 便于检查基线高度等问题.

#let testword = "报告"
#let andbold(w) = [#w#text(weight: "bold", w)]
#let testline(w) = [#w#emph[#andbold(w)]#andbold(w)]

#align(center)[
  #testline(testword) \
  #sans[#testline(testword)] \
  #mono[#testline(testword)]
]
