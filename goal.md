This project plans to migrate the autoreport CLI from the parent directory into a plugin for the parent directory's deepseek harness. The functionality related to autoreportcli/docs/own_features must be preserved. Please fully understand the autoreport CLI, iteratively refine the design plan for this project's autoreportdsh, and then have a subagent review it. After the review is complete, show me your plan; after I agree, dispatch subagents to implement this project in parallel across different branches, with you merging promptly and organizing them to complete development and testing. The APIs used for testing are the same as Claude code cli's API in ~/.claude/ (by the way, we are still very far from testing), using OpenRouter stealth/ox-alpha. Read carefully 2 repos ../deepseek-harness and ../autoreportcli. Note that maybe it is not good to implement all the functions by yourself. If there exisit a plugin for dsh, just use it, implement only what dsh and community plugin do not have. 